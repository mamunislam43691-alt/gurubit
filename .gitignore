# Dependencies
node_modules/
yarn.lock

# Environment variables
.env

# Firebase credentials (NEVER commit — use FIREBASE_SERVICE_ACCOUNT env var on Railway)
config/serviceAccountKey.json
firebase-debug.log
.firebase/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp

# Local runtime data (generated at runtime, not source code)
# data/phone-store.json  — uncomment to exclude from git
# data/guest-store.json  — uncomment to exclude from git

# NOTE: public/css/output.css is intentionally NOT ignored
# Railway build step generates it, but we commit it as fallback
# NOTE: data/catalog.json IS committed — it contains your number pool config
